import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  footer: {
      padding: 10,
      justifyContent: 'center',
      alignItems: 'center',
      textAlign: 'center',
      textDecoration: 'none',
  },
  icon: {
      marginLeft: 10,
    //   display: 'flex',
    //   justifyContent: 'center',
    //   alignItems: 'center',
  }
}));
